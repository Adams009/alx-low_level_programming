#include "main.h"
/**
 * _isupper - function that checks for uppercase character.
 *
 * @c: the int for uppercase
 *
 * Return: return 1 or 0
 */
int _isupper(int c)
{
	if
		(c >= 'A' && c <= 'Z')
		{
			return (1);
		}
	return (0);
}
