#include "main.h"
/**
 * _strlen -  a function that returns the length of a string.
 *
 * @S: the inter to read length
 *
 * Return: Always (0)
 */
int _strlen(char *s)
{
	int z;
	int a = 0;

		for (z = 0; z[s] != '\0'; z++)
			a++;
	return (a);
}
