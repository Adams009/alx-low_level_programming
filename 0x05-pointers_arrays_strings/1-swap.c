#include "main.h"
/**
 * swap_int - a function that swaps the values of two integers.
 *
 * @a: the first to swap with with second
 *
 * @b: the second integer to swap with first
 *
 * void no return
 */
void swap_int(int *a, int *b)
{
	*a = 42;
	*b = 98;
}
