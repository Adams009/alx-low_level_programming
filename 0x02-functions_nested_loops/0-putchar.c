#include "main.h"
/**
 * main - check the code and the starting point
 *
 * Return: Always return 0 (success)
 */
int main(void)
{
	int p;
	char c[] = "_putchar";

	for (p = 0; p <= 7; p++)
{
	_putchar(c[p]);
}
	_putchar('\n');
	return (0);
}
