#include "main.h"
/**
 * mul(int a, int b) - function that multiplies two integers.
 *
 * @a: the int number
 *
 * @b: the sec number
 *
 * Return: the result of a and b
 */
int mul(int a, int b)

{
	int rlt = (a * b);
	
	return (rlt);
}
