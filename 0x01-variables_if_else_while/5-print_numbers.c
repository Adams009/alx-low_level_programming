#include <stdio.h>
/**
 * main - your first room where program starts
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int first = 0;
	int second = 9;

	while
		(first <= second);
	putchar(first);
	first++;
	putchar('\n');
	return (0);
}
