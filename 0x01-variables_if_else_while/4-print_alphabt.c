#include <stdio.h>
/**
 * main - your first room where program starts
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char first;

	for
		(first = 'a'; first <= 'z'; first++)
	if
		(first != 'q' && first != 'e')
	{
	putchar(first);
	}
	putchar('\n');
	return (0);
}
